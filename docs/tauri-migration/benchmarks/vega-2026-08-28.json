{
  "measuredAt": "2026-08-28",
  "device": {
    "port": "COM4",
    "board": "CATS Vega",
    "firmwareVersion": "3.1.0",
    "telemetryFirmwareVersion": "1.2.0"
  },
  "profileSnapshot": {
    "methodology": "Five consecutive read-only version and bulk-configuration transactions over the same connected Vega; timings exclude process startup and the initial 250 ms wake delay.",
    "v1Milliseconds": [333.9, 330.4, 335.1, 333.7, 332.9],
    "v2Milliseconds": [221.2, 214.6, 216.7, 217.8, 248.9],
    "v1MedianMilliseconds": 333.7,
    "v2MedianMilliseconds": 217.8
  },
  "simulation": {
    "command": "sim",
    "available": false,
    "boardResponse": "UNKNOWN COMMAND, TRY 'HELP'",
    "reason": "The sim command is compiled only into CATS development firmware.",
    "v2BehaviorAfterFix": "The packaged GUI reports simulation_unavailable immediately instead of waiting for the five-minute inactivity timeout."
  },
  "serialHandover": {
    "failureBeforeFix": "A transient zero-byte Windows serial read caused the first V2 connection after another serial client to report Board connection closed.",
    "fix": "Continue waiting within the existing command deadline after a zero-byte read.",
    "verification": "Five consecutive V1 profile-read to V2 hardware-smoke handovers passed on COM4."
  }
}
